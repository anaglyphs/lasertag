This is a Unity project with Unity's official MCP server enabled. Its `mcp__unity_mcp__Unity_*` tools may be dynamically exposed rather than shown in the initial tool list. Before claiming the MCP server is unavailable, discover or inspect the available tools for that prefix and make a harmless read-only call such as `Unity_ReadConsole`. Warn the user only if those tools are unavailable or the verification call fails.

Read README.md
